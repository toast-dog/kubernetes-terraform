# Talos v1.14 replaced the old machine.install block
apiVersion: v1alpha1
kind: UnattendedInstallConfig
installer:
  image: ${talos_image}
provisioning:
  diskSelector:
    match: disk.dev_path == "/dev/vda"
